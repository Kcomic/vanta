// Stub for 'server-only' in Vitest (node env).
// The real package throws at import time in non-RSC environments;
// in tests the pure-logic exports (enforceRole, AuthError) are safe to import.
export {};
